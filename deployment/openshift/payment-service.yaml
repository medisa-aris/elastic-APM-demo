apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: elastic-apm-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: image-registry.openshift-image-registry.svc:5000/elastic-apm-demo/demo-payment-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: demo-config
          env:
            - name: OTEL_SERVICE_NAME
              value: demo-payment-service
            - name: PORT
              value: "8081"
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 5
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: elastic-apm-demo
spec:
  selector:
    app: payment-service
  ports:
    - port: 8081
      targetPort: 8081
